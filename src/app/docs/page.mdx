# Welcome to the Docs

Here’s some code:

```js
// JavaScript example
const greet = (name) => {
  console.log(`Hello, ${name}!`);
};
```

// HTML example

```html
<div class="container">
  <h1>Hello, World!</h1>
</div>
```

**Explanation:**

- **Frontmatter (`---`):** Defines metadata for the MDX page.
- **Code Blocks:** Demonstrates JavaScript, HTML, and CSS with proper language annotations for syntax highlighting.

---

## **6. Final Steps**

### **6.1. Install All Dependencies**

Ensure all necessary packages are installed.

```bash
npm install
# or using pnpm
pnpm install
```
